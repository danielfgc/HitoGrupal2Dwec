import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hitogrupal2dwec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
